<h6 class="h4">Account Registration</h6>


{{#if failure_message}}
    <div class="error-msg mgT2">
        <h2 class="h6">{{failure_message}}</h2>
    </div>
{{/if}}
<form id="signup" class=" auth-form" method="post" action="/auth/register">

    <p class="h5">Estimated time to complete your registration is 2 Minutes</p>
    <p class="h5 mgT1">Please complete the following form using English alphabet characters only.</p>
    <p class="h5 mgT1"> Please complete the form below by using your first name and surname as they are displayed in your identity document.</p>
    <p class="h5 mgT1">Please make sure that emails from <em class="link">support@enkryptfinance.com</em> do not get marked as spam so that you can receive all communication emails seamlessly.
        </p>
    <h5 class="h5 mgV2">Personal Details</h5>

    <div class="grid">
        <div class="grid-item2_4">
            <div class="form-input-box">
                <input id="first" class="form-input" name="firstName" placeholder="First Name"/>
            </div>
            <div class="form-input-box">
                <input id="middle" class="form-input"  name="middleName" placeholder="Middle Name"/>

            </div>
            <div class="form-input-box">
                <input id="last" class="form-input" name="lastName" placeholder="Last Name"/>

            </div>
        </div>
        <div class="grid-item2_4">
            <div class="form-input-box">
               <select id="country" name="country" class="form-input">
                   {{#each country}}
                       <option>{{name}}</option>
                   {{/each}}
               </select>

            </div>
            <div class="form-input-box">
                <input class="form-input" id="email" type="email" name="email" placeholder="Email"/>

            </div>
            <div class="form-input-box">
                <input class="form-input" id="password" name="password" type="password" placeholder="Password"/>

            </div>
        </div>
    </div>
            <div class="form-input-box">
                <select name="code" id="code" class="form-input">
                    {{#each country}}
                        <option value="{{code}}">{{name}}&nbsp; +{{code}}</option>
                    {{/each}}
                </select>

            </div>
            <div class="form-input-box width50">
                <input class="form-input" id="phone" type="tel" name="phone" placeholder="phone -- 805 567 5556 "/>
            </div>
    <div>
        <label for="plan" class="h6">Select Plan</label>
        <div class="form-input-box">
            <select id="plan" name="plan" class="form-input">
                {{#each plan}}
                    <option value="{{_id}}">{{title}}</option>
                {{/each}}
            </select>

        </div>
    </div>
  <div class="mgT2">
      <input type="checkbox"  name="terms" id="terms">
      <label class="h5 mgL2" for="terms">I agree to receive your newsletters, company news, and product updates.</label>
  </div>
    <button id="authBtn"   class="mgT2 btn btn-purple ">Register</button>
    <p class="h6 mgV2">Already got an Account? <a href="/auth/login" class="link">Login</a></p>
</form>